# 07 Repo Structure

Current intended structure:

- `docs/` — the canonical documentation for the repository

As implementation is rebuilt, the next expected code structure should be reintroduced only when it clearly matches the repository role.

Future code should be organized around things like:
- input alignment
- unsupervised modeling
- state evaluation
- model refresh

Do not reintroduce old hybrid directories just because they existed before.
