# event_governance

M03-owned event taxonomy, effect-model, impact-window, and event-family evidence helpers.

This package is tooling for `model_03_event_state`. It may prepare or review event-family evidence used by M03 event probability functions, but it is not a separate model layer and must not emit runtime component-control actions such as warn, cap, block, reduce, or flatten.
