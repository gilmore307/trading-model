# model_outputs

Model-output generators that transform accepted upstream feature tables into point-in-time model output rows.

Current package:

- `model_01_market_regime/` — continuous MarketRegimeModel V1 market-state vector.
