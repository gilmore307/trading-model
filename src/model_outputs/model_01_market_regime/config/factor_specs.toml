# MarketRegimeModel V1 factor specification.
#
# This file owns factor membership, signal direction, reducer choice, and
# standardization defaults. The Python generator owns only point-in-time rolling
# standardization and execution mechanics.
#
# Supported reducers:
# - bounded_mean: tanh(mean(zscores) / 2)
# - bounded_abs_mean: tanh(mean(abs(zscores)) / 2)
#
# Supported aggregations:
# - flat: reduce all eligible signals directly.
# - bucketed_mean: first average eligible signals by bucket, then reduce bucket scores.
#
# Signal groups support either:
# - columns = ["exact_column_name", ...]
# - symbols = ["spy", ...] plus suffixes = ["return_20d", ...]
#
# direction = 1 means higher raw signal increases the factor.
# direction = -1 means higher raw signal decreases the factor.

[standardization]
lookback = 120
min_history = 20
std_floor = 1e-8
z_clip = 5.0
min_signal_coverage = 0.5

[[factors]]
name = "trend_factor"
reducer = "bounded_mean"
aggregation = "bucketed_mean"

[[factors.groups]]
symbols = ["spy", "qqq", "iwm", "dia", "rsp"]
suffixes = ["return_5d", "distance_to_ma20", "ma20_slope_5d"]
direction = 1
min_history = 20

[[factors.groups]]
symbols = ["spy", "qqq", "iwm", "dia", "rsp"]
suffixes = ["return_20d", "distance_to_ma50", "ma_alignment_score"]
direction = 1
min_history = 60

[[factors]]
name = "volatility_stress_factor"
reducer = "bounded_mean"
aggregation = "flat"

[[factors.groups]]
symbols = ["spy", "qqq", "iwm", "dia", "rsp", "vixy"]
suffixes = ["realized_vol_20d", "ewma_vol", "atr_pct_14d"]
direction = 1
min_history = 30

[[factors.groups]]
symbols = ["spy", "qqq", "iwm", "dia", "rsp", "vixy"]
suffixes = ["realized_vol_20d_percentile_252d", "realized_vol_20d_zscore_252d"]
direction = 1
min_history = 60

[[factors.groups]]
symbols = ["vixy"]
suffixes = ["return_5d", "distance_to_ma20"]
direction = 1
min_history = 20

[[factors.groups]]
symbols = ["vixy"]
suffixes = ["return_20d", "ma_alignment_score"]
direction = 1
min_history = 60

[[factors]]
name = "correlation_stress_factor"
reducer = "bounded_mean"
aggregation = "flat"

[[factors.groups]]
columns = ["market_state_avg_abs_return_corr_20d", "market_state_avg_abs_return_corr_60d", "market_state_avg_return_corr_20d", "market_state_avg_return_corr_60d"]
direction = 1
min_history = 30

[[factors]]
name = "credit_stress_factor"
reducer = "bounded_mean"
aggregation = "flat"

[[factors.groups]]
columns = ["hyg_lqd_30m", "hyg_lqd_distance_to_ma20", "hyg_lqd_ma_alignment_score"]
direction = -1
min_history = 20

[[factors.groups]]
columns = ["hyg_lqd_realized_vol_20d_ratio"]
direction = 1
min_history = 30

[[factors]]
name = "rate_pressure_factor"
reducer = "bounded_mean"
aggregation = "flat"
# High value means duration/rate pressure: long-duration bonds weakening versus short-duration bonds.
# TLT strength can also indicate risk-off safe-haven demand; that may become a separate factor later.

[[factors.groups]]
columns = ["tlt_shy_30m", "tlt_shy_distance_to_ma20", "tlt_shy_ma_alignment_score", "ief_shy_30m", "ief_shy_distance_to_ma20", "ief_shy_ma_alignment_score"]
direction = -1
min_history = 20

[[factors]]
name = "dollar_pressure_factor"
reducer = "bounded_mean"
aggregation = "flat"

[[factors.groups]]
columns = ["uup_spy_30m", "uup_spy_distance_to_ma20", "uup_spy_ma_alignment_score"]
direction = 1
min_history = 20

[[factors.groups]]
symbols = ["uup"]
suffixes = ["return_5d", "distance_to_ma20"]
direction = 1
min_history = 20

[[factors.groups]]
symbols = ["uup"]
suffixes = ["return_20d", "ma_alignment_score"]
direction = 1
min_history = 60

[[factors]]
name = "commodity_pressure_factor"
reducer = "bounded_mean"
aggregation = "flat"
# High value means commodity-related assets are becoming a dominant market driver.
# It is not automatically bearish: energy/industrial commodity leadership can be risk-on or inflation-led,
# while GLD leadership versus SPY may be safe-haven pressure. Future versions may split this into
# commodity_strength_factor, inflation_pressure_factor, and safe_haven_commodity_factor.

[[factors.groups]]
symbols = ["dbc", "gld", "slv", "uso", "dba", "cper"]
suffixes = ["return_5d", "distance_to_ma20"]
direction = 1
min_history = 20

[[factors.groups]]
symbols = ["dbc", "gld", "slv", "uso", "dba", "cper"]
suffixes = ["return_20d", "ma_alignment_score"]
direction = 1
min_history = 60

[[factors.groups]]
columns = ["gld_spy_30m", "xle_spy_30m", "uso_dbc_30m", "cper_dbc_30m"]
direction = 1
min_history = 20


[[factors]]
name = "breadth_factor"
reducer = "bounded_mean"
aggregation = "flat"

[[factors.groups]]
columns = ["sector_observation_positive_return_1d_pct", "sector_observation_positive_return_5d_pct", "sector_observation_above_ma20_pct", "sector_observation_above_ma50_pct", "sector_observation_above_ma200_pct", "rsp_spy_30m", "rsp_spy_distance_to_ma20"]
direction = 1
min_history = 20

[[factors]]
name = "risk_appetite_factor"
reducer = "bounded_mean"
aggregation = "flat"

[[factors.groups]]
columns = ["qqq_spy_30m", "iwm_spy_30m", "rsp_spy_30m", "hyg_lqd_30m", "xly_xlp_30m", "bitw_spy_30m"]
direction = 1
min_history = 20

[[factors.groups]]
columns = ["tlt_spy_30m", "gld_spy_30m", "uup_spy_30m", "vixy_spy_30m"]
direction = -1
min_history = 20
